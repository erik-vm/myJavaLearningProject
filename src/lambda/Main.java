package lambda;

public class Main {
    public static void main(String[] args) {
/*
        String name = "Erik";
        char symbol = '!';
        char symbol2 = '?';
        MyInterface myInterface = (x, y) -> {    // x = name // y = symbol
            System.out.println("Hello World!");
            System.out.println("It's a nice day " + x + y);
        };
        MyInterface myInterface1 = (x,y) ->{
            System.out.println(x + ", how you doing" + y);
        };
        myInterface.message(name, symbol);
        myInterface1.message(name,symbol2);
        }*/

        MyFrame myFrame = new MyFrame();
    }
}


